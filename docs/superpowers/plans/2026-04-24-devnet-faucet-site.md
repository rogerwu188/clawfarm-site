# Devnet Faucet Site Implementation Plan (Superseded)

This plan is superseded by `docs/superpowers/plans/2026-04-25-devnet-faucet-gateway.md`.

Reason: the masterpool faucet contract now separates the recipient `user` account from the signing `payer` account. The old plan described a browser wallet-signed transaction flow and frontend RPC selector, which no longer matches the approved design.

Do not execute this superseded plan. Use the Gateway-sponsored implementation plan instead.
